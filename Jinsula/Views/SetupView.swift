//
//  SetupView.swift
//  Jinsula
//
//  Family-only configuration screen (bands, contacts, units, and the lock).
//  Skeleton placeholder.
//

import SwiftUI

struct SetupView: View {
    @EnvironmentObject private var model: AppModel

    var body: some View {
        Text("Setup — coming soon")
            .font(Theme.headline())
            .padding()
    }
}
